# {{ project_name }}

## Tech Stack

- FastAPI
- {% if is_tortoise %}Tortoise ORM{% else %}SQLAlchemy{% endif %}
- MySQL / Redis（测试与生产由共享基础设施提供）
- JWT Authentication

## Project Structure

```
{{ project_name }}/
├── app/
├── tests/
├── deploy/
│   ├── Dockerfile
│   ├── docker-compose.yml
│   ├── .env.test
│   ├── deployment.yaml
│   └── README.md
├── .env.example
├── Makefile
└── requirements.txt
```

## Quick Start（本机）

```bash
cp .env.example .env
pip install -r requirements.txt
make dev
```

- Swagger: http://localhost:8000/docs

## 部署

测试走 Compose，生产走 K8s，说明见 [`deploy/README.md`](deploy/README.md)。

```bash
# 测试机：先 source /etc/profile 注入真值，再
docker network create {{ project_name_snake }}-network   # 若尚无
make up
```

## License

MIT
