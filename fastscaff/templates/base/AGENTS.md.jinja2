# {{ project_name }} — Agent 指南

约定与实现冲突时以本文件与现有模块为准，不要另起一套。

## 1. 原则

- **显式优于隐式，确定优于可变**：默认值、错误语义、控制流必须一眼可读；结构用强类型固定，行为用固定机制扩展，不靠约定猜测或模糊文本路由。
- **边界一次定型**：HTTP / Redis / 上游 JSON 在模块边界解析成强类型，内部不传松散 `dict` 当结构体。
- **控制流类型化**：状态码、错误码等用常量或 Enum；禁止靠子串、前缀、`in dict` 做分支。
- **失败可观测**：可预期失败走业务错误路径并打日志；禁止裸 `except: pass` / 无日志的吞异常。
- **默认值克制**：默认值是领域语义。`None`/空串/空列表不是合法状态时收紧签名，而不是处处 `or []`。
- **一事一处**：同一流程一条主路径；差异用配置、依赖注入或已有扩展点，不复制主循环。
- **禁止魔法**：影响分支或契约的数字、字符串、状态名必须先落在常量 / Enum / `ErrorDef`，禁止在业务里散落字面量；日志与展示文案除外。
- **注释克制**：能命名说清的不写注释；只在复杂意图处补一句。中文、说意图不复述代码；方法须有短 docstring，不写实现细节或废话。禁止文件头模块注释；标点极克制，原则上只用逗号，末尾不加句号。注释不替代类型与命名。
- **测试克制**：只测有业务或契约意义的行为；禁止为覆盖率堆用例，禁止 mock 外部依赖来「假绿」。写测试与跑测试都要克制，改动无关时不要全量空转。
- **拒绝异味**：重复逻辑、过长函数、参数泥团、用布尔旗织网、暂时性 `v1`/`先跑通`、为通过检查而 `# type: ignore` / `Any` 逃逸——发现则收紧设计，不叠加补丁。

## 2. 对外契约

### 响应与错误

- 统一信封：`{ "code", "message", "data" }`，成功 `code = 0`。
- 业务错误（`AppError` / `ErrorDef`）对外多为 **HTTP 200 + 非零 code**；未捕获异常与真实 5xx 才用 HTTP 5xx。
- 抛错优先 `ErrorDef.new` / `wrap`（`app/exceptions/codes.py`）。`cause` 只进日志，不回客户端。
- 新增错误先加 `ErrorDef`，禁止散落魔法数字。

### 日志

- stdout JSON；公共字段：`time` / `level`（`WARN`）/ `msg` / `source`。
- Access：`msg="access log"`，含 `method` `path` `query` `status` `latency` `ip` `request` `response`；body 上限 4096，超出加 `...[truncated]`；`/health`、`/metrics` 跳过。
- 请求上下文用 `bind_context`：`trace_id`、`user_id`；不要另造平行关联键名。
- 预期失败的 `error` 保留可读信息，禁止静默丢掉异常。

## 3. 怎么改行为

分层职责：API 只做路由与入出参；业务在 Service（`registry`）；持久化在 Repository；DTO 在 Schema；表在 Model。横切走现有 middleware / utils / exceptions。

{% if is_sqlalchemy %}SQLAlchemy：请求级 session 由中间件管理，业务异常经 handler 后仍会 commit——半截写入勿 `flush` 后再抛。{% endif %}

找扩展点的顺序：

1. 新接口 → `api/v1/endpoints` + `router`，逻辑进 Service
2. 新持久化 → Repository / Model，不在 API 里写查询
3. 新业务错误 → `exceptions/codes.py`
4. 横切（限流、缓存等）→ 现有 `utils` / `middleware`
5. {% if with_celery %}异步任务 → `app/tasks/jobs`{% else %}长时间任务 → 外部队列，勿阻塞事件循环{% endif %}
{% if with_rbac %}
6. 权限策略 → Casbin / `enforcer`，勿在 handler 硬编码角色表
{% endif %}

## 4. 测试

- 默认少写：只测真正有风险或构成契约的路径（关键业务规则、错误码/信封）。getter、纯转发、框架已保证的行为不写测。
- **禁止 mock**（含 AsyncMock / patch 掉仓库或 HTTP 客户端来伪造成功）。测真实协作；依赖过重则缩小测面或改为可本地跑的集成，而不是用假对象堆断言。
- 断言对准契约：信封 `code` / `data` 与稳定业务结果；不测实现细节、日志文案、私有结构。
- 失败路径只保留有意义的几条（校验失败、资源不存在等），不为每个分支复制粘贴。
- 跑测试同样克制：改哪测哪；`make test` 全量留给提交前或明确需要时，禁止每次小改都无脑全跑。
- 禁止靠删测试、放宽断言或 mock 出绿来交差。

## 5. 质量门禁

- 能力闭环：校验、错误语义、日志、相关的并发/幂等、可验证手段。禁止以 `MVP` / `先跑通` 只做 happy path。
- 类型完整；禁止无说明的 `Any`、用裸 `dict` 冒充固定字段结构体。查找表用 `dict[K, V]` 正当，勿为此改成冗长 `if/elif`。
- 配置经 `Settings` / 入参下沉；密钥与 `.env` 真值不进仓库；`deploy/.env.test` 只允许 `${VAR}` 占位。依赖变更写回 `requirements.txt`。

## 6. 禁止事项

- API 层写业务规则，或绕过 Repository 直接操持久化（除非改的就是既有例外处）
- 用正文子串或未类型化魔法字符串做业务分支
- `try/except` 后静默返回且不打日志
- 为模糊调用方堆静默默认值，却不收紧上游契约
- 把日志改成与现有字段方案不兼容的另一套 schema（除非同步改采集侧）
- 合并前无测试支撑的行为变更，或靠删测试 / 放宽断言 / mock「变绿」
